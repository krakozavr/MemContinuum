import XCTest

final class DeleteGateTests: XCTestCase {
    func testDeleteRemovesFile() throws {
        // fixture stub; content doesn't matter, only that this path exists
        // on disk for memlint's tested_by existence check.
    }
}
