{"id": "path-01", "kind": "path", "query": "src/storage/dedup/store.py", "expect": ["TOP-101", "TOP-104", "CON-301"], "notes": "TOP-101's code_refs names the src/storage/dedup/ directory (this file is under it); TOP-104's code_refs names this exact file (the GC sweep it documents lives here); CON-301 'Dedup Store' implemented_by names three of this file's own functions. TOP-101 and TOP-104 also come back nested under CON-301's governed_by, so the set is the union, not three independent hits."}
{"id": "path-02", "kind": "path", "query": "src/storage/checksum.py", "expect": ["TOP-101", "TOP-102"], "notes": "TOP-101's code_refs lists this exact file alongside its directory prefix (the hash function is called out as part of what 'content-addressed' means); TOP-102 is this file's own topic (which hash algorithm). No concept covers this file. Tests that a directory-prefix ref and an exact-file ref can both legitimately name the same path."}
{"id": "path-03", "kind": "path", "query": "src/storage/chunker.py", "expect": ["TOP-103"], "notes": "Single clean match: only TOP-103 (content-defined chunking) names this file, and no concept's implemented_by/tested_by touches it. A control case with no ranking ambiguity at all."}
{"id": "path-04", "kind": "path", "query": "src/storage/trash.py", "expect": ["TOP-105"], "notes": "Single match: TOP-105's code_refs names this exact file, nothing else does."}
{"id": "path-05", "kind": "path", "query": "src/storage/versions.py", "expect": ["TOP-106"], "notes": "Single match: TOP-106's code_refs names this exact file, nothing else does."}
{"id": "path-06", "kind": "path", "query": "src/sync/conflict/resolver.py", "expect": ["TOP-107", "TOP-108", "CON-302"], "notes": "TOP-107's code_refs names this exact file (conflict resolution policy lives here); CON-302 'Conflict Resolver' implemented_by names two of this file's functions, and CON-302's own governed_by pulls in TOP-108 (conflict detection) even though TOP-108's own code_refs points at the sibling vector_clock.py, not this file -- a topic reachable only through the concept, not through any direct code_refs match."}
{"id": "path-07", "kind": "path", "query": "src/sync/conflict/vector_clock.py", "expect": ["TOP-108", "TOP-107", "CON-302"], "notes": "Mirror of path-06 from the sibling file in the same concept's owner_boundary: TOP-108 binds directly here, and the same three-id set comes back regardless of which of the two files in CON-302's boundary is being edited -- the concept's chain travels with the whole boundary, not just one file."}
{"id": "path-08", "kind": "path", "query": "src/sync/merge_queue.py", "expect": ["TOP-109"], "notes": "Single match: TOP-109 (merge-queue retry backoff) names this exact file. No concept covers it. Deliberately the sibling of path-09/10 (upload retry backoff) -- same shape of decision, different file, to test that a path query doesn't spuriously pull in the look-alike topic just because the vocabulary is similar."}
{"id": "path-09", "kind": "path", "query": "src/network/upload/pipeline.py", "expect": ["TOP-110", "TOP-113", "CON-303"], "notes": "TOP-110's code_refs names this exact file; CON-303 'Upload Pipeline' implemented_by names two of this file's functions, and its governed_by pulls in TOP-113 (large-file streaming) even though TOP-113's own code_refs points at the sibling streaming.py, not this file -- concept-level injection again, as in path-06/07."}
{"id": "path-10", "kind": "path", "query": "src/network/streaming.py", "expect": ["TOP-113", "TOP-110", "CON-303"], "notes": "Mirror of path-09 from the sibling file: TOP-113 binds directly here, and the same three-id set comes back as from pipeline.py."}
{"id": "path-11", "kind": "path", "query": "src/network/throttle.py", "expect": ["TOP-111"], "notes": "Single match: TOP-111 (bandwidth throttle) names this exact file. No concept covers it -- deliberately NOT part of CON-303's implemented_by, even though the upload pipeline calls it, per that concept's own 'not this concept' boundary note."}
{"id": "path-12", "kind": "path", "query": "src/network/mobile_throttle.py", "expect": ["TOP-112"], "notes": "Single match: TOP-112 (mobile data cap) names this exact file."}
{"id": "path-13", "kind": "path", "query": "src/auth/oauth.py", "expect": ["TOP-114", "TOP-115", "TOP-116", "CON-304"], "notes": "TOP-114's code_refs names this exact file; CON-304 'Auth Token Manager' implemented_by names this file's device_code_flow function, and its governed_by pulls in BOTH TOP-115 (refresh policy) and TOP-116 (token storage) even though neither's own code_refs names this file -- the widest concept-expansion case in the corpus, four ids from one file."}
{"id": "path-14", "kind": "path", "query": "src/auth/tokens.py", "expect": ["TOP-115", "TOP-116", "TOP-114", "CON-304"], "notes": "TOP-115 and TOP-116 both bind directly here (both name this exact file, since refresh and storage are implemented in the same module); CON-304 also matches (implemented_by names two of this file's functions) and pulls in TOP-114 via governed_by. Same four-id set as path-13 from the sibling file in the same concept boundary."}
{"id": "path-15", "kind": "path", "query": "src/watch/debounce.py", "expect": ["TOP-117"], "notes": "Single match: TOP-117 (debounce window) names this exact file."}
{"id": "path-16", "kind": "path", "query": "src/watch/rename.py", "expect": ["TOP-118"], "notes": "Single match: TOP-118 (inode-based rename detection) names this exact file."}
{"id": "path-17", "kind": "path", "query": "src/cli/config.py", "expect": ["TOP-119", "TOP-120"], "notes": "Both TOP-119 (file format) and TOP-120 (precedence order) name this exact file, since both are implemented in the same config loader. No concept covers cli/. Tests that two genuinely different decisions about the same file both surface, not just one."}
{"id": "path-18", "kind": "path", "query": "src/security/encryption.py", "expect": ["TOP-121"], "notes": "Single match: TOP-121 (client-side encryption) names this exact file."}
{"id": "path-19", "kind": "path", "query": "src/security/symlinks.py", "expect": ["TOP-122"], "notes": "Single match: TOP-122 (symlink policy) names this exact file."}
{"id": "path-20", "kind": "path", "query": "tests/test_dedup.py", "expect": ["CON-301", "TOP-101", "TOP-104"], "notes": "This is a TEST file, not an implementation file: no topic's own code_refs names anything under tests/, so it matches only through CON-301's tested_by entry (concept path-matching checks implemented_by AND tested_by alike), which then pulls in CON-301's own governed_by chain (TOP-101, TOP-104). Without the concept, editing this file would surface nothing at all -- the case for including tested_by in path matching at all."}
{"id": "kw-01", "kind": "question", "query": "why does the dedup store key chunks by content hash instead of by file", "expect": ["TOP-101"], "notes": "Shares real vocabulary (chunks, hash, file, store) with TOP-101's own title and body -- a keyword search should find this one."}
{"id": "kw-02", "kind": "question", "query": "why did the project move from SHA-256 to BLAKE3 for the content hash", "expect": ["TOP-102"], "notes": "Names both algorithms verbatim, which appear in TOP-102's current-link ruling and rationale text."}
{"id": "kw-03", "kind": "question", "query": "why are chunk boundaries content-defined instead of fixed size", "expect": ["TOP-103"], "notes": "Echoes TOP-103's own title closely."}
{"id": "kw-04", "kind": "question", "query": "when are orphaned chunks garbage collected in the dedup store", "expect": ["TOP-104"], "notes": "'orphan'/'garbage collect' echo TOP-104's title and area; also close to INC-201's own domain, but the incident's title is about a race/corruption bug, not the GC policy itself, so TOP-104 is the intended single answer."}
{"id": "kw-05", "kind": "question", "query": "how long do deleted files stay recoverable in trash", "expect": ["TOP-105"], "notes": "Shares 'deleted', 'trash', 'recoverable'-adjacent vocabulary with TOP-105."}
{"id": "kw-06", "kind": "question", "query": "how many old versions of a file does the app keep", "expect": ["TOP-106"], "notes": "Shares 'versions', 'keep' vocabulary with TOP-106's title and ruling."}
{"id": "kw-07", "kind": "question", "query": "why do failed chunk uploads retry with exponential backoff", "expect": ["TOP-110"], "notes": "Echoes TOP-110's own title closely; deliberately similar in shape to TOP-109 (merge-queue backoff) to test that 'chunk upload' language, not just 'retry backoff', picks the right one of the two."}
{"id": "kw-08", "kind": "question", "query": "what's the default upload bandwidth throttle", "expect": ["TOP-111"], "notes": "Shares 'bandwidth', 'throttle', 'upload' vocabulary with TOP-111."}
{"id": "kw-09", "kind": "question", "query": "why does the CLI use OAuth device-code flow instead of an API key", "expect": ["TOP-114"], "notes": "Echoes TOP-114's own title and its rejected alternative (a pasted API key) closely."}
{"id": "kw-10", "kind": "question", "query": "why is the CLI config file TOML instead of YAML", "expect": ["TOP-119"], "notes": "Names both formats verbatim, which appear in TOP-119's current-link ruling and rationale text."}
{"id": "kw-11", "kind": "question", "query": "why are symlinks synced as symlinks instead of their target content", "expect": ["TOP-122"], "notes": "Echoes TOP-122's own title closely."}
{"id": "kw-12", "kind": "question", "query": "what file-size threshold triggers streaming instead of loading the whole file into memory", "expect": ["TOP-113"], "notes": "Shares 'file', 'threshold', 'streaming', 'memory' vocabulary with TOP-113."}
{"id": "kw-13", "kind": "question", "query": "what went wrong during the merge-queue outage", "expect": ["INC-206"], "notes": "Shares 'merge-queue', 'outage' vocabulary with INC-206's own title; targets an incident rather than a topic, unlike most of this set."}
{"id": "kw-14", "kind": "question", "query": "what caused the token refresh rate-limit incident", "expect": ["INC-202"], "notes": "Shares 'token', 'refresh', 'rate-limit' vocabulary with INC-202's own title."}
{"id": "para-01", "kind": "question", "query": "My spouse and I each modified the family budget spreadsheet at the same moment from different laptops while offline, and now our folder has a slightly differently labeled duplicate rather than everything combined into a single sheet -- is that what the software is supposed to do?", "expect": ["TOP-107"], "notes": "Paraphrases 'a conflicting edit keeps both copies' with zero shared vocabulary against BOTH TOP-107's indexed text (title+body+current ruling+rationale) AND its full raw record (frontmatter, tags, alternatives, evidence, the superseded L1 link) -- the surface bench/runners/keyword_baseline.py actually searches; verified by direct token-set diff, not by inspection. Fix round (Grok 8): 'version'/'behave'/'both' all leaked through fields TestParaphraseIndependence's indexed-text-only check never read (alternatives, the superseded L1 link) or that its stopword list happened to mask ('both', the ruling's own concept word -- 'keeps both copies'); all three replaced (duplicate/supposed-to-do/each), zero overlap now verified against the full raw file, not just the indexed subset. Deliberately about the RESOLUTION outcome (a duplicate exists), not detection, to test disambiguation against TOP-108."}
{"id": "para-02", "kind": "question", "query": "How does the software work out that a modification made with no internet at all and one made while online actually clashed, given that no pair of machines' internal timekeeping can be trusted to agree on right now?", "expect": ["TOP-108"], "notes": "Paraphrases 'conflicts are detected with vector clocks, not timestamps' with zero shared vocabulary. Deliberately about the DETECTION mechanism (unreliable clocks), not the resolution outcome, to test disambiguation against TOP-107."}
{"id": "para-03", "kind": "question", "query": "Something that could not be folded into how matters stand right now gets revisited a little later, but the wait before each subsequent revisit grows instead of staying constant -- what governs that growing wait?", "expect": ["TOP-109"], "notes": "Paraphrases the merge-queue's exponential backoff with zero shared vocabulary against TOP-109's full raw record, not just its indexed text (fix-round finding, Grok 8 class: 'staying fixed' originally leaked the word 'fixed' through TOP-109's own `evidence` field, which quotes INC-206's 'a short fixed interval' -- not read by TestParaphraseIndependence's indexed-text-only check, but read by keyword_baseline.py's full-file search; swapped for 'constant', verified zero overlap against the full file). Normatively phrased ('what governs...'), matching the CURRENT exponential-backoff ruling, not INC-206 (a fixed, non-growing 2-second retry interval during an outage) -- INC-206 describes the opposite of a growing wait, so it cannot be an equally good answer here. Deliberately avoids 'upload'/'send'/'transfer' so it isn't mistaken for TOP-110, the look-alike upload-retry topic with the same backoff shape."}
{"id": "para-04", "kind": "question", "query": "Sending a piece of a large item was not delivered on the first try, and the client seemed to sit idle for a bit before making the next try -- what decides the length of that idle gap?", "expect": ["TOP-110"], "notes": "Paraphrases the upload chunk retry backoff with zero shared vocabulary. Deliberately uses 'sending' language (not 'merge') to disambiguate against TOP-109."}
{"id": "para-05", "kind": "question", "query": "Should something stop an idle app from silently moving a large amount of information over a channel that charges by the byte?", "expect": ["TOP-112"], "notes": "M4 fix round (Codex 5): the original wording ('my cellular bill went up sharply after one night... not been near my device') retold INC-205's own overnight/unattended-drain story closely enough that INC-205 was an equally defensible answer, penalized by the single-id key. Reworded as a general POLICY question ('should something stop...'), stripped of any specific-incident narrative (no night/bill/unattended framing), so TOP-112 (the ruling itself) is the only right answer -- a question about what SHOULD happen, not a retelling of what went wrong once. Zero shared vocabulary against TOP-112's full raw record (not just its indexed text): 'link' in the prior wording matched the YAML key `links:` present in every topic's frontmatter, replaced with 'channel', verified zero overlap."}
{"id": "para-06", "kind": "question", "query": "Once I've signed in, where on my laptop does the piece of information that proves my identity actually end up sitting?", "expect": ["TOP-116"], "notes": "Paraphrases 'tokens live in the OS keychain' with zero shared vocabulary (identity/sitting vs. token/keychain/credential). Deliberately about WHERE a token is kept, not how it's obtained, to test disambiguation against TOP-114."}
{"id": "para-07", "kind": "question", "query": "If some background process pokes at a particular spot on disk multiple times back to back almost instantly, does the app really send a distinct outbound transfer for each poke, or does it wait briefly to combine them first?", "expect": ["TOP-117"], "notes": "M4 fix round (Codex 4): the original wording asked whether EVERY DISTINCT item touched in a branch switch gets its own transfer -- TOP-117's actual rule only collapses REPEATED events on the SAME path within 750ms, it says nothing about the count of distinct files in one burst, and the scenario (many different files written once each in a git checkout) is INC-203's own plot, which the vector runner ranked first. Reworded to scope the question to what TOP-117 actually rules on: ONE spot touched repeatedly in quick succession, not many distinct items in one action -- INC-203 (many different files, each written once) is no longer a plausible answer to this shape of question. Zero shared vocabulary against TOP-117's full raw record (not just its indexed text): 'large'/'source' in the prior wording are gone; verified zero overlap."}
{"id": "para-08", "kind": "question", "query": "I moved a directory of photos to a different spot and it synced almost instantly rather than sending everything over from scratch -- how does it know nothing was actually erased and rebuilt somewhere else?", "expect": ["TOP-118"], "notes": "Paraphrases inode-based rename detection with zero shared vocabulary (moved/spot/erased/rebuilt vs. rename/inode/delete/create). Deliberately about the general rule, not its known failure mode, to pair against para-11's INC-204."}
{"id": "para-09", "kind": "question", "query": "If whoever runs the servers gets broken into, can the intruder actually read the contents of what I've backed up?", "expect": ["TOP-121"], "notes": "Paraphrases client-side at-rest encryption with zero shared vocabulary (broken into/intruder/backed-up vs. encrypted/client/ciphertext/breach)."}
{"id": "para-10", "kind": "question", "query": "I have the same spreadsheet saved in three separate folders on my machine -- does keeping three of them around actually take up three times as much room on my computer?", "expect": ["TOP-101"], "notes": "Paraphrases content-addressed dedup with zero shared vocabulary (spreadsheet/folders/room/computer vs. chunk/hash/storage/dedup). The most everyday-language rephrasing in the set -- no technical term from the target survives at all."}
{"id": "para-11", "kind": "question", "query": "After copying an entire set of items to a wiped drive, a pair of completely unconnected items ended up linked together in the app's own bookkeeping as though a single item had simply moved -- what caused that?", "expect": ["INC-204"], "notes": "Paraphrases the inode-reuse false-positive-rename incident with zero shared vocabulary against the target's full raw record (not just its indexed text -- fix round, Grok 8 class: 'two' leaked against the raw file; swapped for 'a pair of', verified zero overlap). Targets the INCIDENT specifically (what went wrong, once), pairing against para-08's TOP-118 (the general rule) -- a system that only ever returns the rule and never the incident (or vice versa) is only half right on this pair."}
{"id": "et-01", "kind": "question", "query": "why did refreshing tokens within the same one-second window trip the identity provider's rate limit", "expect": ["INC-202"], "notes": "Exact-term: 'one-second window' and 'rate limit' are literal phrases from INC-202's own body/title text (indexed FTS content, not frontmatter) -- a keyword search should nail this."}
{"id": "et-02", "kind": "question", "query": "why does a conflicting edit produce a file named like a conflicted copy instead of overwriting the original", "expect": ["TOP-107"], "notes": "Exact-term: 'conflicted copy' is a literal quoted phrase from TOP-107's current (L2) ruling text ('name (conflicted copy, device-b).md') -- indexed via ruling_text, so FTS can match it verbatim."}
{"id": "et-03", "kind": "question", "query": "what is a vector clock used for when detecting a sync conflict", "expect": ["TOP-108"], "notes": "Exact-term/symbol: 'vector clock' is verbatim in TOP-108's own title ('Conflicts are detected with vector clocks, not timestamps') -- the deliberate opposite of para-02, which avoided this exact jargon; here a keyword search should win easily."}
{"id": "et-04", "kind": "question", "query": "what went wrong when the chunk garbage collector raced a concurrent write", "expect": ["INC-201"], "notes": "Exact-term: 'raced a concurrent write' is almost verbatim INC-201's own title ('Chunk garbage collection raced a concurrent write and corrupted the dedup store') -- a near-quote of the record's own indexed title."}
{"id": "et-05", "kind": "question", "query": "why does a file at or above 256MB get streamed instead of read fully into memory", "expect": ["TOP-113"], "notes": "Exact-term: the literal figure '256MB' appears in TOP-113's current ruling text ('A file at or above 256MB is uploaded in bounded-memory streamed chunks'). Unlike kw-12 (same target, no number), this pins the exact threshold value."}
{"id": "et-06", "kind": "question", "query": "what is the default 50MB limit on a metered mobile connection", "expect": ["TOP-112"], "notes": "Exact-term: '50MB' is the literal daily-cap figure in TOP-112's ruling text ('a separate daily data cap (default 50MB) applies'). Distinguishes from TOP-111's own literal figure (5MB/s bandwidth, a different unit and topic) -- the look-alike pair. (Fix round, Grok 14: reworded 'what is the default 50MB limit for on a metered mobile connection' -> this, dropping the ungrammatical dangling 'for'.)"}
{"id": "et-07", "kind": "question", "query": "why does a failed chunk upload stop retrying after 6 attempts", "expect": ["TOP-110"], "notes": "Exact-term: '6 attempts' (ruling text) and 'six tries' (title) are both literal in TOP-110 -- distinguishes from the look-alike TOP-109, whose own literal cap is '5 attempts'/'five tries'."}
{"id": "et-08", "kind": "question", "query": "where does Driftwood store tokens on Windows -- does it use something like Credential Manager", "expect": ["TOP-116"], "notes": "Exact-term: 'Credential Manager' is a literal proper noun in TOP-116's current ruling text ('Keychain on macOS, Credential Manager on Windows, Secret Service on Linux'), corpus-unique."}
{"id": "et-09", "kind": "question", "query": "what's a rolling hash used for when finding chunk boundaries", "expect": ["TOP-103"], "notes": "Exact-term: 'rolling hash' is a literal technical phrase in TOP-103's current ruling text ('using a rolling hash to find boundaries')."}
{"id": "et-10", "kind": "question", "query": "what's a token bucket rate limiter's default upload speed", "expect": ["TOP-111"], "notes": "Exact-term: 'token-bucket' is literal in TOP-111's ruling text ('capped by a token-bucket rate limiter, defaulting to 5MB/s') -- distinguishes from the look-alike TOP-112, which has no such phrase."}
{"id": "et-11", "kind": "question", "query": "what happened when a large git checkout's write burst overwhelmed the sync queue", "expect": ["INC-203"], "notes": "Exact-term: near-verbatim quote of INC-203's own title ('A large git checkout's write burst overwhelmed the sync queue')."}
{"id": "et-12", "kind": "question", "query": "why does the trash keep deleted files recoverable for exactly 30 days before purging them for good", "expect": ["TOP-105"], "notes": "Exact-term: '30 days' and 'purged for good' are literal phrases in TOP-105's ruling text ('trash entries are purged for good after 30 days') -- distinguishes from the look-alike TOP-106 (10 versions, a different figure/topic)."}
